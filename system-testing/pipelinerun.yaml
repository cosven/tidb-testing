apiVersion: tekton.dev/v1beta1
kind: PipelineRun
metadata:
  generateName: systesting-demo-
spec:
  serviceAccountName: system-testing
  pipelineRef:
    name: systesting
  workspaces:
    - name: manifest
      emptyDir: {}
  params:
    - name: run-id
      # value: systesting-2tscale
      value: systesting-demo
  timeout: "6h"
